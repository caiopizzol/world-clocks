{
  "name": "world-clocks",
  "version": "0.1.0",
  "description": "New tab Chrome extension with world clocks and a map",
  "scripts": {
    "lint": "biome check",
    "format": "biome check --write",
    "package": "rm -f world-clocks.zip && zip -r world-clocks.zip manifest.json world-clocks.html world-clocks.js leaflet.js leaflet.css cities.json icons/"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "lefthook": "^2.1.2",
    "semantic-release": "^25.0.3"
  }
}
